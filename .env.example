# Copy to .env and fill in real values.
# Neon (or any Postgres) connection string used by the API and migrations.
DATABASE_URL=postgresql://user:password@host/db?sslmode=require

# Port the API listens on locally (Vercel injects PORT at runtime).
PORT=3000
