# Go workspace root — the API lives in apps/api
.vercel
.env
.env.*.local
node_modules/
server
.playwright-mcp

# web build output (the Go embed directory is populated by `make web`)
apps/web/dist/
apps/api/webfs/dist/*
!apps/api/webfs/dist/.gitkeep
